import { type ReactElement } from "react";

/**
 * Reference examples are not shipped in this workspace.
 * Build lesson content in `src/data/blocks.tsx` and `src/data/sections/`.
 */
const exampleBlocks: ReactElement[] = [];

export { exampleBlocks };
